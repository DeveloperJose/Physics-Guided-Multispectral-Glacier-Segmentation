# Gen20 Desktop Baseline (Class Weight Fix)
# Goal: Verify reproducibility of the baseline fix on different hardware.
training_opts:
  run_name: "gen20_desktop_baseline_cw_fix"
loss_opts:
  class_weights: [0.01, 1]
loader_opts:
  batch_size: 8
