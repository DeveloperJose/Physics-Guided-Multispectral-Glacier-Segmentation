# Inherit from the Gen6 baseline for Bilbo
inherit: configs/bilbo/base_gen6.yaml

training_opts:
  run_name: "gen6_synthesis_multiclass"
  dataset_name: "bibek_w512_o64_f1_velocity"

# The Synthesis Model: Combine Physics and Velocity
loader_opts:
  physics_channels: True
  velocity_channels: True
