# Gen13 Multiclass BASE on Bilbo (batch 16).
# No physics or velocity.

training_opts:
  dataset_name: bibek_w512_o64_f1_v2
  run_name: gen13_multi_base_bs16
  epochs: 200
  test_eval_n: 3
  early_stopping: 50

loader_opts:
  batch_size: 16
  physics_channels: false
  velocity_channels: false
