# Inherit from the Gen6 baseline for Frodo
inherit: configs/frodo/base_gen6.yaml

training_opts:
  run_name: "gen6_multiclass_velocity"
  dataset_name: "bibek_w512_o64_f1_velocity"

loader_opts:
  physics_channels: False
  velocity_channels: True
