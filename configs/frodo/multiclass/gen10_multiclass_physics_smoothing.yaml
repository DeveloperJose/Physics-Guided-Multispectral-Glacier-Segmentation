# Gen10 Multiclass with Physics Channels and Label Smoothing (Frodo RTX 2080 Ti)
# Goal: Improve overall multiclass performance with physics channels and label smoothing.

training_opts:
  dataset_name: "bibek_w512_o64_f1_comprehensive_phys64_s1"
  run_name: "gen10_multiclass_physics_smoothing"

loader_opts:
  physics_channels: true

loss_opts:
  label_smoothing: 0.1