# Gen23 Experiment: Debris Ice Baseline with Early Stopping
# Objective: Baseline for the LR sweep, using the current max_lr with the new early stopping.

training_opts:
  run_name: "gen23_dci_baseline_es20_md001"
  early_stopping: 20
  early_stopping_min_delta: 0.001
  # early_stopping: 20 (inherited from train.yaml)
  # early_stopping_min_delta: 0.001 (inherited from train.yaml)

loader_opts:
  # batch_size: 8 (inherited from train.yaml)
  # output_classes: [2] (inherited from train.yaml)
  # robust_scaling: false (inherited from train.yaml)

optim_opts:
  args:
    # lr: 0.0003 (inherited from train.yaml)

scheduler_opts:
  args:
    max_lr: 0.0005 # Current baseline max_lr
