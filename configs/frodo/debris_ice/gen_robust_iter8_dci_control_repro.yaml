# Frodo 1: Reproduction of the 0.47 IoU SOTA Baseline (Linear Scaling)
training_opts:
  run_name: "gen_robust_iter8_dci_control_repro"
  dataset_name: "gen_robust_comprehensive"

loader_opts:
  batch_size: 8
  landsat_channels: true
  dem_channels: true
  spectral_indices_channels: true
  hsv_channels: true
  physics_channels: false
  velocity_channels: false
  robust_scaling: false
