# Gen20 Variation: Robust Scaling
# Goal: Final check on robust scaling with a corrected baseline.
training_opts:
  run_name: "gen20_robust_scaling"
loss_opts:
  class_weights: [0.01, 1]
loader_opts:
  batch_size: 16
  robust_scaling: true
