# Frodo Control: Pure Landsat baseline.
training_opts:
  run_name: "gen_robust_iter7_dci_control_landsat"
  epochs: 200
  early_stopping: 75
  test_eval_aggressive_threshold: 0.0
  dataset_name: "gen_robust_comprehensive"

loader_opts:
  batch_size: 8
  landsat_channels: true
  dem_channels: false
  spectral_indices_channels: false
  hsv_channels: false
  physics_channels: false
  velocity_channels: false
