# Gen24: weight decay 1e-3 with looser early stopping

training_opts:
  run_name: "gen24_wd_1e3_es30_md001"
  early_stopping: 30
  early_stopping_min_delta: 0.001

optim_opts:
  args:
    weight_decay: 0.001

scheduler_opts:
  args:
    max_lr: 0.0005
