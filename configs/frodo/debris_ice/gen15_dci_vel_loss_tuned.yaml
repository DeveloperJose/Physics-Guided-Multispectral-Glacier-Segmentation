training_opts:
  run_name: "gen15_dci_vel_loss_tuned"
  dataset_name: "bibek_w512_o64_f1_v2_velocity"
  epochs: 90
  early_stopping: 10

loader_opts:
  physics_channels: false
  velocity_channels: true

loss_opts:
  use_velocity_loss: true
  velocity_confidence_threshold: 0.6
  velocity_low_speed_threshold: 0.25
  velocity_loss_weight: 0.02
  velocity_loss_warmup_epochs: 5
  velocity_loss_ramp_epochs: 5
  velocity_loss_clip: 1.0
