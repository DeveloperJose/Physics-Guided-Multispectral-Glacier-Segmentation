# Gen13 Debris-Covered Ice Physics + Velocity (Frodo GPU3, w512).
# Both physics and velocity channels enabled; velocity loss remains disabled.

training_opts:
  dataset_name: bibek_w512_o64_f1_v2_phys64_s075_velocity
  run_name: gen13_dci_physvel
  epochs: 200
  test_eval_n: 3
  early_stopping: 50

loader_opts:
  physics_channels: true
  velocity_channels: true
