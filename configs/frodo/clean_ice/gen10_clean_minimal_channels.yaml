# Gen10 Minimal Channel Set for Clean Ice Efficiency (Frodo RTX 2080 Ti)
# Goal: Determine the most efficient set of channels for high Clean Ice IoU, potentially reducing computational overhead.

training_opts:
  dataset_name: "bibek_w512_o64_f1_comprehensive_phys64_s1"
  run_name: "gen10_clean_minimal_channels"

loader_opts:
  hsv_channels: false
  velocity_channels: false
  physics_channels: false