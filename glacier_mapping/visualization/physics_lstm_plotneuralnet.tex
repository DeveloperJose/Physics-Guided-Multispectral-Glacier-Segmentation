
\documentclass[border=8pt, multi, tikz]{standalone}
\usepackage{import}
\subimport{./Layers/}{init}
\usetikzlibrary{positioning}
\usetikzlibrary{3d} %for including external image

\def\ConvColor{rgb:yellow,5;red,2.5;white,5}
\def\ConvReluColor{rgb:yellow,5;red,5;white,5}
\def\PoolColor{rgb:red,1;black,0.3}
\def\UnpoolColor{rgb:blue,2;green,1;black,0.3}
\def\FcColor{rgb:blue,5;red,2.5;white,5}
\def\FcReluColor{rgb:blue,5;red,5;white,4}
\def\SoftmaxColor{rgb:magenta,5;black,7}
\def\SumColor{rgb:blue,5;green,15}

\newcommand{\copymidarrow}{\tikz \draw[-Stealth,line width=0.8mm,draw={rgb:blue,4;red,1;green,1;black,3}] (-0.3,0) -- ++(0.3,0);}

\begin{document}
\begin{tikzpicture}
\tikzstyle{connection}=[ultra thick,every node/.style={sloped,allow upside down},draw={rgb:blue,4;red,1;green,4;black,3},opacity=0.7]
\tikzstyle{copyconnection}=[ultra thick,every node/.style={sloped,allow upside down},draw={rgb:blue,4;red,1;green,1;black,3},opacity=0.7]

\pic{
    RightBandedBox={
        name=input_sequence,
        caption=Input Sequence$\mathbf{X}_{t-T:t}$,
        xlabel={ 3, 3 },
        zlabel=256,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=40,
        width={ 6, 6 },
        depth=40
    }
};

\pic{
    RightBandedBox={
        name=cnn_conv1,
        caption=CNN Conv1$\mathbf{F}_{\theta}$,
        xlabel={ 64, 64 },
        zlabel=256,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=35,
        width={ 3, 3 },
        depth=35
    }
};

\pic{
    Box={
        name=cnn_pool1,
        caption= ,
        fill=\PoolColor,
        opacity=0.6,
        height=28,
        width=1.5,
        depth=28
    }
};

\pic{
    RightBandedBox={
        name=cnn_conv2,
        caption=CNN Conv2$\mathbf{G}_{\phi}$,
        xlabel={ 128, 128 },
        zlabel=128,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=24,
        width={ 4, 4 },
        depth=24
    }
};

\pic{
    Box={
        name=cnn_pool2,
        caption= ,
        fill=\PoolColor,
        opacity=0.6,
        height=18,
        width=1.5,
        depth=18
    }
};

\pic{
    RightBandedBox={
        name=cnn_conv3,
        caption=CNN Conv3$\mathbf{H}_{\psi}$,
        xlabel={ 256, 256 },
        zlabel=64,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=14,
        width={ 5, 5 },
        depth=14
    }
};

\pic{
    RightBandedBox={
        name=lstm_input,
        caption=LSTM Input$\mathbf{x}_t$,
        xlabel={ 32, 32 },
        zlabel=256,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=20,
        width={ 2, 2 },
        depth=20
    }
};

\pic{
    RightBandedBox={
        name=lstm_cell1,
        caption=LSTM Cell 1$\mathbf{h}_1$,
        xlabel={ 64, 64 },
        zlabel=128,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=18,
        width={ 3, 3 },
        depth=18
    }
};

\pic{
    RightBandedBox={
        name=lstm_cell2,
        caption=LSTM Cell 2$\mathbf{h}_2$,
        xlabel={ 64, 64 },
        zlabel=64,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=16,
        width={ 3, 3 },
        depth=16
    }
};

\pic{
    RightBandedBox={
        name=lstm_cell3,
        caption=LSTM Cell 3$\mathbf{h}_3$,
        xlabel={ 64, 64 },
        zlabel=32,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=14,
        width={ 3, 3 },
        depth=14
    }
};

\pic{
    RightBandedBox={
        name=physics_constraint,
        caption=Physics Layer: $\nabla \cdot \mathbf{v} = 0$, $\frac{\partial \mathbf{v}}{\partial t} + (\mathbf{v} \cdot \nabla)\mathbf{v} = -\nabla p + \nu \nabla^2 \mathbf{v}$,
        xlabel={ 128, 128 },
        zlabel=32,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=12,
        width={ 4, 4 },
        depth=12
    }
};

\pic{
    RightBandedBox={
        name=feature_fusion,
        caption=Fusion Layer: $\mathbf{z} = [\mathbf{f}_{cnn}, \mathbf{h}_{lstm}, \mathbf{p}_{physics}]$,
        xlabel={ 256, 256 },
        zlabel=32,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=10,
        width={ 6, 6 },
        depth=10
    }
};

\pic{
    RightBandedBox={
        name=seg_head,
        caption=Seg Head: $\mathbf{y}_{seg}$,
        xlabel={ 128, 128 },
        zlabel=64,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=8,
        width={ 4, 4 },
        depth=8
    }
};

\pic{
    RightBandedBox={
        name=vel_head,
        caption=Vel Head: $\mathbf{v}_{pred}$,
        xlabel={ 64, 64 },
        zlabel=64,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=6,
        width={ 3, 3 },
        depth=6
    }
};

\pic{
    RightBandedBox={
        name=seg_output,
        caption=Output: $\hat{\mathbf{y}}$,
        xlabel={ 3, 3 },
        zlabel=128,
        fill=\ConvColor,
        bandfill=\ConvReluColor,
        height=6,
        width={ 2, 2 },
        depth=6
    }
};

\draw [connection]  (input_sequence-east) -- node {\midarrow} (cnn_conv1-west);

\draw [connection]  (cnn_conv1-east) -- node {\midarrow} (cnn_pool1-west);

\draw [connection]  (cnn_pool1-east) -- node {\midarrow} (cnn_conv2-west);

\draw [connection]  (cnn_conv2-east) -- node {\midarrow} (cnn_pool2-west);

\draw [connection]  (cnn_pool2-east) -- node {\midarrow} (cnn_conv3-west);

\draw [connection]  (input_sequence-east) -- node {\midarrow} (lstm_input-west);

\draw [connection]  (lstm_input-east) -- node {\midarrow} (lstm_cell1-west);

\draw [connection]  (lstm_cell1-east) -- node {\midarrow} (lstm_cell2-west);

\draw [connection]  (lstm_cell2-east) -- node {\midarrow} (lstm_cell3-west);

\draw [connection]  (lstm_cell3-east) -- node {\midarrow} (physics_constraint-west);

\draw [connection]  (physics_constraint-east) -- node {\midarrow} (feature_fusion-west);

\draw [connection]  (cnn_conv3-east) -- node {\midarrow} (feature_fusion-west);

\draw [connection]  (feature_fusion-east) -- node {\midarrow} (seg_head-west);

\draw [connection]  (seg_head-east) -- node {\midarrow} (vel_head-west);

\draw [connection]  (seg_head-east) -- node {\midarrow} (seg_output-west);

\end{tikzpicture}
\end{document}
